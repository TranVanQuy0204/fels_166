<%= form_for [:admin, @category] do |f| %>
  <%= render "/shared/error_messages", object: f.object %>
  <div class="row">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="row">
    <%= f.submit yield(:button_text), class: "btn btn-primary",
      title: yield(:button_text) %>
  </div>
<% end %>
